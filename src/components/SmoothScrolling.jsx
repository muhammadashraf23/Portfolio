"use client";

// lenis is not yet installed — using a passthrough wrapper.
// To restore smooth scroll: `pnpm add lenis` and re-import ReactLenis.
export default function SmoothScrolling({ children }) {
  return <>{children}</>;
}
